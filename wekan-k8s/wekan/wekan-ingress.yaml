---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: wekan-ingress
  namespace: wekan-project
spec:
  rules:
  - host: wekan.localhost
    http:
      paths:
      - path: /
        backend:
          serviceName: wekan
          servicePort: 8080
